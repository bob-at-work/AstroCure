<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingTop="30dp"
    android:paddingBottom="30dp">

    <ImageView
        android:id="@+id/color_card_back"
        android:layout_width="380dp"
        android:layout_height="560dp"
        android:src="@drawable/color_therapy_back_card_1"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent=".5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_percent=".7" />

    <TextView
        android:id="@+id/predictedText"
        android:layout_width="380dp"
        android:layout_height="560dp"
        android:background="@drawable/color_therapy_back_card_1"
        android:gravity="center"
        android:letterSpacing=".1"
        android:lineSpacingExtra="10dp"
        android:padding="20dp"
        android:text="@string/dummy_1"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent=".5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_percent=".7" />

    <ImageView
        android:id="@+id/close"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="10dp"
        android:src="@drawable/ic_video_close"
        app:layout_constraintEnd_toEndOf="@id/predictedText"
        app:layout_constraintTop_toTopOf="@id/predictedText" />

</androidx.constraintlayout.widget.ConstraintLayout>